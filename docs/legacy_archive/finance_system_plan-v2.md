# Personal Modular Finance & Productivity System

> 개인용 · 소수 지인용 self-hosted 내부 관리 시스템

---

## 1. 프로젝트 개요

본 프로젝트는 **가계부 / 정기 구독 관리**를 시작으로, 향후 **Scheduler, TODO, 프로젝트·외주 관리, 정산 시스템**까지 확장 가능한 **모듈형 개인 생산성 시스템**을 구축하는 것을 목표로 한다.

- 대상 사용자: **본인 + 허용된 소수 지인**
- 배포 형태: **GitHub 공개 (Self-hosted only)**
- 운영 철학: "서비스가 아닌 내부 툴", "유지보수 부담 최소화"

---

## 2. 핵심 설계 원칙

### 2.1 서비스가 아닌 프레임워크
- SaaS 형태 ❌
- 중앙 서버 ❌
- 데이터 수집 ❌
- 사용자 직접 배포 / 직접 설정

### 2.2 Core / Module / Plugin 분리
- **Core**: 인프라 레이어 (절대 최소 변경)
- **Modules**: 실제 기능 단위 (안정적)
- **Plugins**: 실험/확장 영역 (깨져도 무방)

### 2.3 모듈 중심 구조
- 모듈별로 frontend/backend 코드를 한 곳에 모아 관리
- 모듈 추가/삭제는 폴더 단위로 간단하게
- 커뮤니티가 독립적으로 모듈 개발 가능
- 코어 코드를 건드리지 않고 기능 확장

### 2.4 개인정보 보호 우선
- DB, AI, 외부 API는 **사용자가 직접 API Key 등록**
- 프로젝트는 어떤 개인정보도 수집하지 않음

---

## 3. 기술 스택

### Backend
- **TypeScript + Node.js**
- Express or Fastify
- 다양한 DB 지원 (PostgreSQL, Supabase, MongoDB)

### Frontend
- **React + TypeScript**
- Vite
- 공통 UI 컴포넌트 (Core에서 제공)

### 개발 환경
- **Monorepo**: pnpm workspace
- 빌드: tsup, vite
- 배포: Docker Compose (선택), Railway, Cloudflare

---

## 4. 전체 아키텍처 개요

```
Client (Web / App)
   ↓
Core Layer
 ├ Auth (Google OAuth + Whitelist)
 ├ DB Connector (Multi-provider)
 ├ Module Loader (자동 스캔)
 ├ Event Bus
 ├ AI Abstraction
 └ Common UI Components
   ↓
Modules (자동 로드)
 ├ Frontend (React Components)
 └ Backend (API Routes)
   ↓
Plugins (Optional)
```

---

## 5. 디렉터리 구조

```
my-finance-system/
├── pnpm-workspace.yaml
├── package.json
├── docker-compose.yml
├── .env.example
│
├── packages/
│   └── core/                     # 핵심 공통 코드
│       ├── auth/                 # Google OAuth, Whitelist
│       ├── db/                   # DB 추상화 (Postgres/Supabase/MongoDB)
│       ├── types/                # 공통 타입 정의
│       ├── utils/                # 공통 유틸
│       └── ui/                   # 공통 UI 컴포넌트
│
├── apps/
│   ├── api/                      # Backend API Server
│   │   ├── src/
│   │   │   ├── index.ts          # 메인 서버
│   │   │   ├── loader/           # 모듈 자동 로더
│   │   │   ├── middleware/
│   │   │   └── plugins/          # 백엔드 플러그인
│   │   │       ├── scheduler/
│   │   │       └── ai/
│   │   └── package.json
│   │
│   └── web/                      # Frontend React App
│       ├── src/
│       │   ├── main.tsx
│       │   ├── App.tsx
│       │   └── loader/           # 프론트 모듈 로더
│       └── package.json
│
├── modules/                      # 기능 모듈들
│   ├── ledger/                   # 가계부 모듈
│   │   ├── module.json           # 모듈 메타데이터
│   │   ├── frontend/             # UI/UX
│   │   │   ├── index.tsx
│   │   │   ├── pages/
│   │   │   └── components/
│   │   ├── backend/              # API 로직
│   │   │   ├── index.ts
│   │   │   ├── routes.ts
│   │   │   ├── service.ts
│   │   │   └── schema.ts
│   │   └── types/
│   │
│   ├── subscription/             # 구독 관리 모듈
│   │   ├── module.json
│   │   ├── frontend/
│   │   ├── backend/
│   │   │   └── calendar.ts       # Google Calendar 연동
│   │   └── types/
│   │
│   └── example/                  # 모듈 개발 템플릿
│
└── docs/
    ├── architecture.md
    ├── module-guide.md           # 모듈 개발 가이드
    └── deployment.md
```

---

## 6. 인증 및 접근 제어

### 6.1 인증 방식
- Google OAuth 로그인만 허용
- 비밀번호 시스템 미사용

### 6.2 접근 제어
- 로그인 후 이메일 기준 **Whitelist 체크**
- `allowed_users` 테이블에 등록된 계정만 접근 가능

---

## 7. 데이터베이스 정책

### 7.1 멀티 DB 지원
사용자가 선택 가능:
- **Local PostgreSQL** (권장)
- **SQLite** (간단한 배포)
- **Supabase** (클라우드)
- **MongoDB** (NoSQL 선호 시)

### 7.2 설정 예시

```env
# Local PostgreSQL
DB_PROVIDER=local
DATABASE_URL=postgresql://localhost:5432/mydb

# Supabase
DB_PROVIDER=supabase
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_KEY=xxx

# MongoDB
DB_PROVIDER=mongodb
MONGODB_URI=mongodb://localhost:27017/mydb
```

### 7.3 DB 추상화
Core에서 DB Provider를 추상화하여 모듈은 DB 종류를 신경쓰지 않음

---

## 8. AI 연동 정책

- AI Provider는 추상화 레이어로 분리
- 사용자 직접 API Key 등록

```env
AI_PROVIDER=gemini
AI_API_KEY=xxxx
```

AI는 **계산 주체가 아닌 분석/요약/해설 역할**로만 사용

---

## 9. Module 시스템 설계

### 9.1 Module 메타 정보 (module.json)

```json
{
  "name": "ledger",
  "version": "1.0.0",
  "displayName": "가계부",
  "description": "수입/지출 관리",
  "icon": "💰",
  "routes": {
    "frontend": "/ledger",
    "api": "/api/ledger"
  },
  "permissions": ["db:read", "db:write"],
  "dependencies": [],
  "enabled": true
}
```

### 9.2 모듈 자동 로딩

**Backend:**
```typescript
// apps/api/src/index.ts
import { loadModules } from './loader';

const modules = await loadModules('../../modules');
modules.forEach(mod => {
  app.use(`/api/${mod.name}`, mod.backend);
});
```

**Frontend:**
```typescript
// apps/web/src/App.tsx
import { loadModules } from './loader';

const modules = await loadModules('../../modules');

<Router>
  {modules.map(mod => (
    <Route 
      path={mod.config.routes.frontend}
      element={<mod.frontend />}
    />
  ))}
</Router>
```

### 9.3 모듈 개발 규칙
- Module 간 직접 import 금지
- Core → Module 의존 금지
- Module은 자신이 소유한 DB 테이블만 접근 가능
- 모듈 폴더 하나에 frontend/backend/types 모두 포함

### 9.4 모듈 설치/제거
```bash
# 커뮤니티 모듈 설치
git clone https://github.com/someone/finance-crypto-module modules/crypto

# 제거
rm -rf modules/crypto
```

---

## 10. Scheduler 설계

- Scheduler는 Backend Plugin으로 구현
- 모듈은 작업만 등록

예시:
- 월간 가계부 요약
- 구독 결제일 체크
- 외주 정산 알림

---

## 11. 기본 제공 Modules (MVP)

### 11.1 Ledger (가계부)
- 날짜
- 금액 (+ / -)
- 카테고리
- 메모
- 결제 수단

### 11.2 Subscription (정기 구독)
- 서비스명
- 금액
- 결제 주기 (월/년)
- 결제일
- Google Calendar 연동

---

## 12. UI 정책

- UI는 Core/Module 로직과 완전 분리
- Core에서 기본 UI 컴포넌트 제공 (Button, Modal, Layout 등)
- 모듈은 Core UI를 활용하여 일관된 디자인 유지
- 사용자는 자유롭게 UI 교체 가능

---

## 13. 배포 방식

### 13.1 개발 모드
```bash
pnpm dev              # 전체 실행
pnpm dev:api          # Backend만
pnpm dev:web          # Frontend만
```

### 13.2 프로덕션 배포

**Docker Compose (Self-hosted):**
```bash
docker-compose up -d
```

**Railway / Cloudflare:**
- Backend: Railway에 apps/api 배포
- Frontend: Cloudflare Pages에 apps/web 배포

**빌드 과정:**
```bash
pnpm build            # 모든 모듈 코드가 자동 포함됨
```

## 14. Scheduler 설계

- Scheduler는 Backend Plugin으로 구현
- 모듈은 작업만 등록
- 통합 서비스와 연계 가능

예시:
- 월간 가계부 요약
- 구독 결제일 체크
- 외주 정산 알림
- Google Drive 자동 백업
- Slack 리포트 전송

### 15.1 왜 필요한가?
모듈 개발자가 UI/UX를 처음부터 만들 필요 없이, Core에서 제공하는 컴포넌트를 사용하여 빠르고 일관된 인터페이스를 구축할 수 있도록 지원

**장점:**
- ✅ 통일된 디자인 시스템
- ✅ 즉시 사용 가능한 컴포넌트
- ✅ 반응형 자동 처리
- ✅ 다크모드 자동 지원
- ✅ 접근성 자동 처리

### 15.2 Core UI 구성

```
packages/core/ui/
├── components/          # 기본 컴포넌트
│   ├── Button.tsx
│   ├── Input.tsx
│   ├── Select.tsx
│   ├── Modal.tsx
│   ├── Table.tsx
│   ├── Card.tsx
│   ├── Tabs.tsx
│   ├── Form.tsx
│   ├── DatePicker.tsx
│   └── Dropdown.tsx
│
├── layouts/            # 레이아웃 컴포넌트
│   ├── PageLayout.tsx   # 표준 페이지
│   ├── FormLayout.tsx   # 폼 페이지
│   └── ListLayout.tsx   # 리스트 페이지
│
├── hooks/              # 공통 Hooks
│   ├── useForm.ts
│   ├── useModal.ts
│   ├── useTable.ts
│   └── useNotification.ts
│
└── index.ts
```

### 15.3 사용 예시

```typescript
// 모듈에서 Core UI 사용
import { PageLayout, Table, Button, Card } from '@core/ui';

export default function CryptoList() {
  return (
    <PageLayout 
      title="암호화폐 추적"
      actions={<Button variant="primary">추가</Button>}
    >
      <Card>
        <Table
          columns={[
            { key: 'name', label: '이름' },
            { key: 'price', label: '가격' }
          ]}
          data={cryptos}
          onRowClick={handleClick}
        />
      </Card>
    </PageLayout>
  );
}
```

### 15.4 고급 컴포넌트

**DataTable (정렬, 필터, 페이지네이션 내장):**
```typescript
<DataTable
  columns={columns}
  data={data}
  searchable
  sortable
  filterable
  pagination
/>
```

**FormBuilder (JSON 스키마로 폼 자동 생성):**
```typescript
<FormBuilder
  schema={{
    fields: [
      { name: 'amount', type: 'number', label: '금액', required: true },
      { name: 'category', type: 'select', label: '카테고리' }
    ]
  }}
  onSubmit={handleSubmit}
/>
```

## 16. 모듈 마켓플레이스 & 관리 시스템

### 16.1 앱 내 모듈 관리 페이지
사용자가 UI를 통해 모듈을 관리할 수 있는 대시보드 제공:

**기능:**
- 설치된 모듈 목록 조회
- 모듈 활성화/비활성화 토글
- 인증된 모듈 검색 및 설치
- 모듈 업데이트 확인 및 실행
- 모듈 제거

### 16.2 공식 웹사이트 (Marketplace + Docs)

**웹사이트 구조:**
```
https://your-finance-system.dev/
├── /                          # 홈 (프로젝트 소개)
├── /docs/                     # 문서
│   ├── /getting-started
│   ├── /user-guide/          # 사용자 가이드
│   ├── /developer/           # 개발자 가이드
│   └── /deployment/          # 배포 가이드
├── /marketplace/             # 🆕 마켓플레이스
│   ├── /                     # 모듈 목록
│   ├── /module/:id          # 모듈 상세
│   └── /stats               # 통계 대시보드
├── /blog/                    # 업데이트 소식
└── /community/               # 커뮤니티
```

**기술 스택:**
- Docusaurus (React 기반 문서 사이트 생성기)
- Cloudflare Pages (무료 배포)
- Algolia (무료 검색 기능)

**주요 기능:**
- 📦 모듈 검색 및 필터링
- 📊 실시간 다운로드 통계
- 📸 스크린샷 갤러리
- 📝 상세 README 표시
- ⭐ 인기 모듈 랭킹
- 📈 다운로드 추이 차트

### 16.3 공식 모듈 레지스트리

**레지스트리 구조:**
```
github.com/your-org/module-registry
├── modules.json              # 인증된 모듈 목록
├── categories.json           # 카테고리 정의
├── modules/
│   ├── ledger.json          # 개별 모듈 상세 정보
│   ├── crypto-tracker.json
│   └── todo.json
├── stats/
│   └── downloads.json        # 다운로드 통계
├── submissions/              # 새 모듈 제출 템플릿
└── .github/workflows/
    └── update-stats.yml      # 자동 통계 업데이트
```

**modules.json 예시:**
```json
{
  "version": "1.0.0",
  "lastUpdated": "2025-01-21T12:00:00Z",
  "modules": [
    {
      "id": "crypto-tracker",
      "name": "crypto-tracker",
      "displayName": "암호화폐 추적",
      "description": "비트코인, 이더리움 실시간 가격 추적",
      "author": "community-user",
      "verified": true,
      "category": "finance",
      "tags": ["crypto", "bitcoin"],
      "icon": "₿",
      "repository": "https://github.com/user/crypto-tracker",
      "version": "2.1.0",
      "minCoreVersion": "1.0.0",
      "downloads": 523,
      "stars": 42,
      "updatedAt": "2025-01-15T00:00:00Z",
      "detailUrl": "https://raw.githubusercontent.com/.../crypto-tracker.json"
    }
  ]
}
```

### 16.4 모듈 인증 프로세스

**제출 절차:**
1. 개발자가 GitHub에 모듈 레포 생성
2. module-registry에 PR 제출
3. 메인테이너 검토:
   - 코드 리뷰
   - 보안 체크
   - module.json 검증
   - 악성 코드 검사
4. 승인 시 modules.json에 추가
5. 공식 웹사이트 마켓플레이스에 자동 표시

**보안 정책:**
- 인증된 모듈만 마켓플레이스에 표시
- GitHub Actions를 통한 자동 검증
- 악성 코드 패턴 스캔
- 필수 필드 검증

### 16.5 모듈 설치 방식

**사용자 관점:**
1. 공식 웹사이트 또는 앱 내 마켓플레이스에서 모듈 검색
2. "설치" 버튼 클릭
3. 백엔드가 해당 모듈의 GitHub 레포에서 자동 다운로드
4. 자동으로 modules/ 폴더에 설치
5. 서버 재시작 또는 핫 리로드로 즉시 사용 가능

**기술적 흐름:**
```
웹사이트/앱 → API: /api/system/modules/install
                ↓
Backend: git clone {repository}
                ↓
Backend: 검증 (module.json, 보안 체크)
                ↓
Backend: modules/{name}/ 에 설치
                ↓
Module Loader: 자동 감지 및 로드
                ↓
통계 서버: 다운로드 카운트 증가
```

### 16.6 통계 수집 시스템

**다운로드 추적:**
```typescript
// 앱에서 설치 시 통계 전송
await fetch('https://your-finance-system.dev/api/track-install', {
  method: 'POST',
  body: JSON.stringify({ moduleName: 'crypto-tracker' })
});
```

**통계 저장:**
- Cloudflare Workers + KV Storage
- GitHub Actions로 일별 집계
- 실시간 업데이트

**통계 대시보드 제공:**
- 전체 모듈 수
- 총 다운로드 수
- 인기 모듈 Top 10
- 다운로드 추이 차트
- 카테고리별 분포

### 16.7 모듈 업데이트 & 제거

**업데이트:**
- 레지스트리와 설치된 버전 비교
- 업데이트 가능 시 알림 표시
- 원클릭 업데이트 (git pull)
- 변경 사항 (changelog) 표시

**제거:**
- UI에서 제거 버튼 클릭
- modules/{name} 폴더 삭제
- 자동으로 모듈 로더에서 제외

### 16.8 모듈 ON/OFF 토글

```json
// module.json
{
  "name": "ledger",
  "enabled": true  // ← false로 변경 시 비활성화
}
```

- UI 토글로 즉시 활성화/비활성화
- 비활성화된 모듈은 로드되지 않음
- 재설치 없이 기능 켜고 끄기 가능

## 17. 배포 및 업데이트 전략

### 17.1 쉬운 Self-hosted 목표

**핵심 원칙:**
- Self-hosted를 극도로 간단하게 만들기
- 일반 사용자도 쉽게 설치 가능
- 자동 업데이트 지원
- 호스팅 서비스 없음 (법적/운영 부담 제거)

### 17.2 웹 기반 설치 마법사 (시놀로지 스타일)

**설치 플로우:**
```
1. 프로젝트 다운로드/클론
   ↓
2. Docker Compose / npm install (기본만)
   ↓
3. 서버 실행 → 웹 브라우저 자동 열림
   ↓
4. 웹 설정 마법사 (모든 설정 여기서)
   ↓
5. "설치 시작" 버튼 클릭
   ↓
6. 백그라운드에서:
   - DB 마이그레이션
   - 의존성 설치
   - 모듈 초기화
   - 관리자 계정 생성
   ↓
7. 완료 → 로그인 화면
```

**사용자 경험:**
- CLI 명령어 불필요
- 모든 설정을 웹 UI에서 입력
- 실시간 설치 진행 상황 확인
- 에러 발생 시 즉시 알림 및 재시도

### 17.3 설치 화면 구성

**화면 1: 시작 화면**
- 시스템 요구사항 표시
- 소요 시간 안내 (약 10-15분)
- 필요한 항목 미리보기

**화면 2: 설정 입력 (한 페이지)**
```
1. 관리자 계정 (필수)
   - 이름, 이메일, 비밀번호

2. 데이터베이스 (필수)
   - SQLite (권장, 자동 설정)
   - PostgreSQL (고성능)
   - Supabase (클라우드)

3. AI 기능 (선택)
   - Provider 선택 (Gemini, OpenAI, Claude, Ollama)
   - API Key 입력
   - API Key 발급 링크 제공

4. Google 연동 (선택)
   - OAuth Client ID/Secret
   - 설정 가이드 링크 제공

5. 모듈 선택 (선택)
   - 가계부 (권장) ✓
   - 구독 관리 (권장) ✓
   - TODO
   - 프로젝트 관리
   - [모두 건너뛰기] 버튼
   - 💡 모듈을 선택하지 않으면 튜토리얼 화면이 표시됩니다
```

**화면 3: 설치 진행**
- 단계별 진행 상황 표시
  1. 설정 검증
  2. 데이터베이스 연결
  3. 데이터베이스 마이그레이션
  4. 의존성 설치
  5. 선택한 모듈 다운로드 및 설치
  6. 관리자 계정 생성
  7. 최종 설정
- 실시간 로그 출력
- 진행률 바 표시
- WebSocket으로 실시간 업데이트

**화면 4: 완료**
- 설치된 항목 요약
- 다음 단계 안내
- "로그인하러 가기" 버튼

### 17.4 설치 방법

**Docker Compose:**
```bash
git clone https://github.com/your-org/finance-system.git
cd finance-system
docker-compose up -d

# 브라우저 자동 열림
→ http://localhost:3000/install
```

**수동 설치:**
```bash
git clone https://github.com/your-org/finance-system.git
cd finance-system
npm install
npm run start

# 브라우저 자동 열림
→ http://localhost:3000/install
```

**Railway/Cloudflare 배포:**
1. GitHub 연결
2. 자동 배포
3. 첫 접속 시 /install로 자동 리다이렉트
4. 웹 마법사로 설정 완료

### 17.5 설정 관리 (웹 UI)

**설정 페이지 구조:**
```
설정
├── 일반 설정
│   ├── 프로필
│   ├── 언어
│   └── 테마
├── AI 설정
│   ├── Provider 변경
│   ├── API Key 관리
│   └── 연결 테스트
├── 데이터베이스
│   ├── 연결 정보
│   └── 연결 테스트
├── 통합 서비스
│   ├── Google (OAuth)
│   ├── Notion, Slack, GitHub
│   └── 커스텀 Webhook
├── 모듈 관리
└── 개발자 도구
```

**특징:**
- 모든 설정을 웹 UI에서 변경 가능
- 환경 변수 직접 수정 불필요
- API Key 암호화 저장
- 연결 테스트 기능 제공
- 변경 사항 즉시 반영

### 17.6 자동 업데이트 시스템

**시간대 지정:**
```
설정 → 시스템 → 업데이트
├── 자동 업데이트 활성화
├── 업데이트 주기
│   ├── 매일
│   ├── 매주 (요일 선택)
│   └── 매월
├── 업데이트 시간 (시:분)
├── 업데이트 전 확인 받기
└── 유지보수 모드 사용
```

**자동 업데이트 프로세스:**
```
1. 설정된 시간에 업데이트 체크
   ↓
2. 활성 사용자 확인
   - 있으면 → 연기
   - 없으면 → 진행
   ↓
3. 유지보수 모드 활성화 (선택)
   ↓
4. 백업 생성
   - DB 백업
   - 코드 백업 (Git tag)
   - 설정 파일 백업
   ↓
5. 업데이트 실행
   - git pull (또는 Docker image pull)
   - 의존성 업데이트
   - DB 마이그레이션
   ↓
6. 검증 및 재시작
   ↓
7. 유지보수 모드 해제
   ↓
8. 완료 알림
```

**안전 장치:**
- 활성 사용자 있으면 자동 연기
- 업데이트 전 자동 백업
- 실패 시 자동 롤백
- 유지보수 모드로 데이터 보호
- 관리자에게 실시간 알림

**유지보수 모드:**
- 업데이트 중 일반 사용자 접근 차단
- 관리자는 접근 가능
- "시스템 업데이트 중" 화면 표시
- 예상 완료 시간 안내

**알림 시스템:**
- 업데이트 24시간 전 알림
- 업데이트 1시간 전 경고
- 업데이트 완료 알림
- 실패 시 관리자 긴급 알림

**롤백 기능:**
- 업데이트 전 자동 백업
- 실패 시 자동 롤백
- 수동 롤백 지원
- 백업 목록 관리

**Docker Watchtower 연동:**
```yaml
services:
  watchtower:
    image: containrrr/watchtower
    environment:
      - WATCHTOWER_SCHEDULE=0 0 3 * * *  # 매일 03:00
      - WATCHTOWER_CLEANUP=true
      - WATCHTOWER_NOTIFICATION_URL=${WEBHOOK_URL}
```

**수동 업데이트:**
```
설정 → 시스템 → 업데이트 확인
   ↓
새 버전 발견
   ↓
[지금 업데이트] 버튼 클릭
   ↓
변경 사항 확인
   ↓
업데이트 진행 (백그라운드)
```

**설정 예시:**

일반 사용자 (개인):
- 자동 업데이트: 활성화
- 주기: 매일
- 시간: 새벽 3시
- 유지보수 모드: 활성화

팀 사용 (회사):
- 자동 업데이트: 활성화
- 주기: 매주 일요일
- 시간: 새벽 2시
- 업데이트 전 확인: 활성화
- 유지보수 모드: 활성화

파워유저:
- 자동 업데이트: 비활성화
- 수동으로 필요 시 업데이트

### 17.7 상업화 방어 전략

**Self-hosted가 쉬워서 상업화 실익 없음:**
- 웹 기반 설치로 진입 장벽 제거
- 자동 업데이트로 관리 부담 제거
- 누가 호스팅 서비스 해봤자 돈 안 됨

**브랜드 선점:**
- 공식 웹사이트 (문서 + 마켓플레이스)
- 공식 GitHub, Discord, YouTube
- SEO 최적화로 검색 1위

**빠른 혁신:**
- 지속적인 업데이트
- 포크는 항상 뒤처짐
- 사용자들은 공식 버전 선택

**커뮤니티 락인:**
- Discord 커뮤니티
- 공식 모듈 레지스트리
- 포크해도 생태계 못 가져감

## 18. GitHub 공개 정책

### 18.1 저장소 구조
```
github.com/your-org/
├── finance-system/              # 메인 프로젝트
├── module-registry/             # 인증된 모듈 레지스트리 (JSON)
├── website/                     # 🆕 공식 웹사이트 (Marketplace + Docs)
├── module-ledger/              # 공식 모듈들
├── module-subscription/
└── module-example/             # 모듈 개발 템플릿
```

### 18.2 공개 범위
- 소스 전체 공개 (MIT License)
- 배포본 제공 ❌ (Self-hosted 전제)

### 18.3 프로젝트 철학

**핵심 가치:**
- ✅ 완전 무료 (기능 제한 없음)
- ✅ Self-hosted 우선
- ✅ 개인정보 보호
- ✅ 커뮤니티 중심
- ✅ 투명한 개발

**하지 않는 것:**
- ❌ 프리미엄 기능 없음
- ❌ 유료 모듈 없음
- ❌ 호스팅 서비스 없음
- ❌ 구독 요금 없음
- ❌ 광고 없음
- ❌ 추적 없음

### 18.4 후원 정책

**선택적 후원:**
- GitHub Sponsors 통해 자발적 후원 가능
- 후원 여부와 관계없이 모든 기능 동일
- 후원금은 개발 유지비로 사용 (도메인, 인프라 등)

**후원자 혜택 (비기능적):**
- README에 이름/로고 노출
- Discord 특별 역할
- 개발 로드맵 투표권
- 조기 뉴스레터 구독

**투명성:**
- 월간 재정 보고서 공개
- 수입/지출 투명하게 공개
- 커뮤니티와 함께 의사결정

### 18.5 README 명시 사항
- 개인/소규모 내부용 프로젝트임을 명확히 고지
- Self-hosted 전제
- 기능 요청은 PR만 허용
- 공식 웹사이트 링크
- 커뮤니티 모듈 개발 가이드 제공
- 후원 안내 (강제 아님)

### 18.6 모듈 생태계
- 사용자들이 독립적으로 모듈 개발 가능
- GitHub에 모듈을 공개하여 공유
- 표준 module.json 형식 준수
- PR을 통한 레지스트리 등록
- 커뮤니티 검증 및 리뷰 시스템
- 공식 웹사이트 마켓플레이스에서 검색 및 설치

---

## 19. 단계별 개발 계획

### Phase 1 (Core 기반)
- Monorepo 세팅 (pnpm workspace)
- Core: Auth / Config / DB / Types
- Core UI Component Library 구축
- Module Loader (자동 스캔 시스템)

### Phase 2 (기본 기능)
- Ledger Module (가계부)
- Subscription Module (구독 관리)
- Google Calendar 연동

### Phase 3 (마켓플레이스 & 웹사이트)
- Module Registry 구축 (JSON)
- 공식 웹사이트 개발 (Docusaurus)
  - 마켓플레이스 UI
  - 사용자/개발자 문서
  - 통계 대시보드
- 앱 내 Module Manager 개발
- 모듈 설치/제거/업데이트 시스템
- 통계 수집 시스템

### Phase 4 (배포 최적화)
- CLI 설정 마법사 (npx create-finance-system)
- Railway/Cloudflare 원클릭 템플릿
- Docker 자동 설정 스크립트
- 자동 업데이트 시스템
- 웹 기반 초기 설정 UI

### Phase 5 (확장)
- Scheduler Plugin
- TODO / Project / 외주 정산 모듈
- AI 요약 자동화

## 20. 프로젝트 목표 요약

- ✔ 혼자 써도 가치 있는 시스템
- ✔ 미래의 유지보수를 고려한 구조
- ✔ 개인정보 이슈 없는 공개 프로젝트
- ✔ 개인 생산성 OS로 확장 가능
- ✔ 커뮤니티가 모듈을 자유롭게 개발 가능
- ✔ 모듈 설치/제거가 폴더 단위로 간단함
- ✔ UI를 통한 모듈 관리로 비개발자도 사용 가능
- ✔ 인증 시스템으로 보안 보장
- ✔ 분산화된 모듈 생태계 (WordPress/VSCode 확장 모델)
- ✔ 공식 웹사이트를 통한 마켓플레이스 및 문서 제공
- ✔ Core UI로 모듈 개발 진입 장벽 낮춤
- ✔ 실시간 통계 및 커뮤니티 활성화
- ✔ 쉬운 Self-hosted (원클릭 배포)
- ✔ 자동 업데이트로 관리 부담 최소화
- ✔ 완전 무료 (기능 제한 없음)
- ✔ 선택적 후원으로 지속 가능한 개발
- ✔ 투명한 운영 (재정 보고서 공개)

---

> "이 프로젝트는 완성된 서비스가 아니라,
> 스스로에게 최적화된 도구를 만들기 위한 기반이자,
> 커뮤니티가 함께 확장할 수 있는 플랫폼이다."